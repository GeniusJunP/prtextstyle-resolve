# PrTextStyle Resolve 変換の未完了タスク一覧 (TODO)

以下のタスクは、これまでの調査・実装で「未完了」または「完全な情報が得られていない」として残っている項目です。

## オープンな課題 (未完了)

- [ ] **ストロークアラインメント (Center/Outside/Inside) のフラグ探索 (#30)**
  - 現在すべてのストロークが `"center"` 扱いになっている原因（PremiereのデフォルトがCenterであり、テストコーパス内で差異がないため特定不能）を解消する。
  - Premiere側で意図的に「外側」や「内側」に設定したテスト用プリセットを新規作成し、バイナリ比較によってアラインメントを制御するフラグを特定する。

- [ ] **塗りの不透明度 (Fill Opacity) のフラグ特定**
  - 現在のところ「単色塗りの不透明度」に該当する確実なフィールドが未確認のため、`Alpha1 = 1.0` 固定で出力している。
  - 透明度を持たせた単色プリセットを作成し、フラグを探索する。

- [ ] **シャドウのサブフィールド欠落時の正確なデフォルト値適用**
  - `distance`, `size`, `blur` などの値が欠落している場合（警告: `shadow subfield missing while enabled`）の代替値が現在ゼロ等になっているが、Premiere上での実際の挙動と一致しているか確認する。

- [ ] **サムネイル生成の完全対応 (thumbnail.py)**
  - 現在は塗り（element 1）しかレンダリングされていない。ストローク（element 2）およびシャドウ（element 3+）の描画を追加する。

- [ ] **未インストールフォントの Font/Style 分割処理の改善**
  - 実行環境にフォントが存在しない場合、フォント名が分割できずPostScript名がそのまま使われてしまう問題の解決策（辞書の拡充など）を検討する。

## 完了済みの課題

- [x] **シャドウのスプレッド (Spread / Size) 実装 (#29)** (ElementShape=1 によるアウトラインフォールバック実装完了)
- [x] **グラデーション角度のゼロ基準・符号の検証 (#27)** (Resolve側の反時計回り仕様に合わせた `-90.0 - angle` 補正実装完了)
- [x] **グラデーションの中間点 (Midpoints) の対応** (50%ブレンドストップの自動挿入による近似処理実装完了)
