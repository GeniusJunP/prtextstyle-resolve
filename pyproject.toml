[project]
name = "prtextstyle-resolve"
version = "0.1.0"
dependencies = [
    "flatbuffers",
    "Pillow"
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
pythonpath = ["src"]
